rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, delete, create: if request.auth.uid == userId;
      match /lifts/{lift} {
        allow read, create, delete: if request.auth.uid == userId;
      }
    }
  }
}
